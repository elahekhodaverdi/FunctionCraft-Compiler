def func(a)
    return a - 2;
end

def main()
    a = 3.4;
    pointer = method(:func);

    loop do
        pointer = a;
        c = pointer + 2.1;
        a = 2;
    end

    b = a + 1.1;
    pointer("Mehdi");
end