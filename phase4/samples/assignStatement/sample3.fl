def main()
     int = 10;
     bool = true;
     str = "First";
     list_of_int = [10, 20];
     list_of_str = ["First", "Second", "3rd"];

     copied_int = int;
     copied_bool = bool;
     copied_str = str;
     copied_list_of_int = list_of_int;
     copied_list_of_str = list_of_str;

     puts(copied_int);
     puts(copied_bool);
     puts(copied_str);
     puts(copied_list_of_int);
     puts(copied_list_of_str);

end